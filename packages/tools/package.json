{
  "name": "@investigation-ai/tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed tool interfaces and provider adapters.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsc --watch --pretty",
    "build": "rimraf dist && tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@investigation-ai/shared-types": "workspace:*"
  }
}
